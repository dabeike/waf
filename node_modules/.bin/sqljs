#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sqljs/bin/sqljs" "$@"
  ret=$?
else 
  node  "$basedir/../sqljs/bin/sqljs" "$@"
  ret=$?
fi
exit $ret
